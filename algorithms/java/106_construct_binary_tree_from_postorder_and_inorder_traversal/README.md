## We can do both traversal in one recursion
- using postorder to know the root -> postorder last element of the subtree
- using inorder to know the boundary -> to the root or out of bound

time: O(n)<br>
space: O(height)
